import Vue from 'vue'

export default new Vue()

/* 使用方法
*   import Bus form '@/helper/bus'
*   Bus.$on('test',msg=>{
*     console.log(msg)
*   })
*   Bus.$emit('test'.'hello world')
* */













